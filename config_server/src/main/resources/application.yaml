server:
  port: 8286

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/ritam6/config-server
          clone-on-start: true

logging:
  file:
    name: logs/config-server.log
    path: logs
  level:
    org.springframework.web: "debug"

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${SERVICE_REGISTRY_URL}
  instance:
    hostname: localhost