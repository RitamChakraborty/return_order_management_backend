server:
  port: 8181

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: component-processing
          uri: http://localhost:8081
          predicates:
            - Path=/component-processing/api/**