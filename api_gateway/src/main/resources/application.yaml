server:
  port: 8181

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: component-processing
          uri: lb://component-processing
          predicates:
            - Path=/component-processing/api/**